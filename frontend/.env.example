# Frontend environment variables
# Copy this file to .env and update as needed

# For development with Vite proxy, no custom API URL is needed (uses relative /api paths)
# For production without proxy, set to backend URL e.g. https://api.your-crm.com
# VITE_API_BASE_URL=
